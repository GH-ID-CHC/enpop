﻿# EnPop 开发文档

> EnPop — English + Popup，一个 Windows 全局翻译朗读工具。
>
> 在任何应用中选中英文，按快捷键即弹出翻译浮窗，支持即时朗读。

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术栈](#2-技术栈)
3. [项目结构](#3-项目结构)
4. [系统架构](#4-系统架构)
5. [模块设计](#5-模块设计)
6. [数据流](#6-数据流)
7. [有道翻译 API 接入](#7-有道翻译-api-接入)
8. [配置管理](#8-配置管理)
9. [开发环境搭建](#9-开发环境搭建)
10. [构建与打包](#10-构建与打包)
11. [后续规划](#11-后续规划)

---

## 1. 项目概述

### 1.1 目标

在 Windows 任意应用上，通过全局快捷键选中英文文本：

- 调用有道云翻译 API 进行 **英译中** 翻译
- 在鼠标附近弹出 **气泡浮窗** 显示原文与译文
- 支持 **Edge TTS** 自然语音朗读英文原文

### 1.2 用户场景

| 场景 | 说明 |
|------|------|
| 阅读英文文档/文章 | 浏览器中遇到生词或看不懂的句子，选中呼出翻译 |
| IDE 英文报错或注释 | VS Code / IDEA 中快速查看英文提示的含义 |
| PDF 英文文献 | 学术阅读时即选即译，不打断阅读节奏 |
| 英文邮件/消息 | 快速理解不熟悉的英文表达 |

### 1.3 核心原则

- **最小干扰**：浮窗数秒自动消失，用完即走
- **即选即译**：选中文本 → 按快捷键 → 看到翻译，三步完成
- **离线兜底**：翻译需要网络，但 TTS 离线状态下可降级到 Windows 自带语音

---

## 2. 技术栈

| 层级 | 技术选型 | 版本要求 | 说明 |
|------|----------|----------|------|
| **语言** | Python | >= 3.10 | 开发效率高，生态丰富 |
| **全局热键** | pynput | latest | 监听全局快捷键 |
| **剪贴板操作** | pyperclip | latest | 模拟 Ctrl+C 获取选中文本 |
| **翻译 API** | HTTP + requests | -- | 访问有道云翻译 API |
| **浮窗 UI** | tkinter | Python 内置 | 无边框悬浮窗口，零依赖 |
| **TTS 朗读** | edge-tts | latest | 微软 Edge 在线语音，免费自然 |
| **TTS 降级** | pyttsx3 | latest | 离线时切换 Windows SAPI5 语音 |
| **托盘图标** | pystray | latest | 系统托盘常驻管理 |
| **图标生成** | Pillow | latest | 托盘图标绘制 |
| **配置存储** | JSON (内置 json) | -- | 本地配置文件 |
| **打包工具** | PyInstaller | latest | 打包为单 exe 分发 |

---

## 3. 项目结构

```
enpop/
├── src/
│   ├── __init__.py
│   ├── main.py              # 入口：启动托盘，注册热键
│   ├── hotkey.py             # 全局热键管理
│   ├── capturer.py           # 文本捕获（剪贴板法）
│   ├── translator.py         # 有道翻译 API 调用
│   ├── tts.py                # TTS 朗读引擎封装
│   ├── popup.py              # 气泡浮窗 UI
│   ├── tray.py               # 系统托盘图标与菜单
│   ├── config_manager.py     # 配置读写管理
│   └── constants.py          # 常量与配置默认值
├── assets/
│   ├── icon.png              # 托盘图标
│   └── icon.ico              # Windows 应用图标
├── config.json               # 用户配置文件（运行时生成）
├── requirements.txt          # 依赖清单
├── build.spec                # PyInstaller 打包配置
├── docs/
│   └── 开发文档.md            # 本文件
└── README.md                 # 项目简介
```

---

## 4. 系统架构

```
+------------------------------------------------------------------+
|                     enpop (后台常驻)                               |
|                                                                  |
|  系统托盘图标 ---- 右键菜单：设置 / 停止 / 退出                      |
|                                                                  |
|  全局热键监听（pynput）                                            |
|     |                                                             |
|  +------------------------------------------------------------+  |
|  |              核心工作流                                      |  |
|  |                                                            |  |
|  |  1 Ctrl+Alt+E 触发                                          |  |
|  |     |                                                      |  |
|  |  2 capturer 保存剪贴板 -> 模拟 Ctrl+C -> 读剪贴板           |  |
|  |     |                                                      |  |
|  |  3 translator 调用有道 API（英->中）                         |  |
|  |     |                                                      |  |
|  |  4 popup 显示浮窗（原文 + 译文 + 朗读按钮）                  |  |
|  |     |                                                      |  |
|  |  5 用户点击朗读 -> tts 播放英文原文                          |  |
|  |    或 5秒后 / 按 Esc -> 浮窗自动关闭                         |  |
|  +------------------------------------------------------------+  |
+------------------------------------------------------------------+
```

### 线程模型

由于 pynput 的全局热键监听会阻塞主线程，而 tkinter 的浮窗也需要事件循环，采用**多线程**设计：

| 线程 | 职责 |
|------|------|
| **主线程** | 系统托盘 (pystray)，常驻等待用户操作 |
| **热键线程** | pynput.Listener 全局键盘监听 |
| **浮窗线程** | 热键触发后启动，运行 tkinter 浮窗 |
| **TTS 线程** | 朗读时不阻塞 UI |

> 浮窗线程需要在热键触发时动态创建，用完后销毁，避免多线程竞争。

---

## 5. 模块设计

### 5.1 hotkey.py -- 全局热键

```python
class HotkeyManager:
    """全局热键管理"""

    DEFAULT_HOTKEY = "<ctrl>+<alt>+e"  # 默认 Ctrl+Alt+E

    def __init__(self, callback):
        self.hotkey = self._load_hotkey()
        self.callback = callback  # 热键触发时回调

    def start(self):
        """启动热键监听（阻塞，需在独立线程运行）"""
        from pynput import keyboard
        with keyboard.GlobalHotKeys({self.hotkey: self.callback}) as listener:
            listener.join()

    def update_hotkey(self, new_hotkey):
        """运行时更新热键组合"""
        pass  # 需重启监听

    def _load_hotkey(self):
        """从配置文件读取热键设置"""
        pass
```

#### 热键格式

pynput.GlobalHotKeys 使用字符串表示组合键，例如：

- `"<ctrl>+<alt>+e"` -- Ctrl + Alt + E
- `"<ctrl>+<shift>+t"` -- Ctrl + Shift + T
- `"<ctrl>+<alt>+<shift>+f1"` -- Ctrl + Alt + Shift + F1

支持的修饰键：`<ctrl>`, `<alt>`, `<shift>`, `<cmd>`(Win 键)

### 5.2 capturer.py -- 文本捕获

```python
def capture_selected_text(max_retries: int = 3) -> str:
    """
    捕获当前应用中选中的文本。

    策略：保存剪贴板 -> 模拟 Ctrl+C -> 读剪贴板 -> 恢复剪贴板

    Returns:
        选中文本，失败返回空字符串
    """
    pass
```

#### 关键细节

- 使用 **SendInput API**（替代已弃用的 keybd_event）模拟键盘，兼容性更好
- 只释放 Alt（避免热键 Ctrl+Alt+E 残留导致 Ctrl+Alt+C），**不干预 Ctrl 键**（用户仍按着），保持键状态同步
- 执行 Ctrl+C 后等待 150ms 初始延时，再通过 **轮询方式**（`_wait_for_new_clipboard`）等待剪贴板更新，单次最长等待 500ms，自适应长文本复制耗时
- 最多重试 3 次，每次重试包含固定延时 + 轮询等待
- 捕获完成后延迟 300ms 恢复原始剪贴板内容
- 捕获到空文本时不触发后续流程
class YoudaoTranslator:
    """有道云翻译 API 客户端"""

    API_URL = "https://openapi.youdao.com/api"

    def __init__(self, app_key: str, app_secret: str):
        self.app_key = app_key
        self.app_secret = app_secret

    def translate(self, text: str,
                   from_lang: str = "auto",
                   to_lang: str = "zh-CHS") -> dict:
        """
        调用有道翻译 API。

        Args:
            text: 待翻译文本
            from_lang: 源语言（auto 自动检测）
            to_lang: 目标语言（zh-CHS 简体中文）

        Returns:
            { "code": 0, "translation": [...], "query": "...", ... }
        """
        pass
```

#### API 签名算法

有道使用 appKey + salt + curtime + sign 进行鉴权：

```
sign = sha256(appKey + truncate(text) + salt + curtime + appSecret)
```

其中 truncate(text) 取输入文本前 20 个字符。

### 5.4 tts.py -- 语音朗读

```python
class TTSPlayer:
    """语音朗读引擎封装"""

    def __init__(self, engine: str = "edge"):
        """
        Args:
            engine: "edge"（在线，自然）或 "sapi5"（离线降级）
        """
        self.engine = engine

    def speak(self, text: str, lang: str = "en"):
        """异步朗读文本"""
        pass

    def stop(self):
        """停止朗读"""
        pass

    @property
    def is_speaking(self) -> bool:
        pass
```

### 5.5 popup.py -- 气泡浮窗

```python
class TranslationPopup:
    """翻译结果气泡浮窗"""

    def show(self, original: str, translation: str,
             x: int = None, y: int = None):
        """
        在鼠标附近显示带动态高度的浮窗。

        Args:
            original: 英文原文
            translation: 中文翻译
            x, y: 显示位置（默认鼠标位置）
        """
        pass

    def close(self):
        """关闭浮窗"""
        pass

    @staticmethod
    def _calc_text_height(text: str) -> int:
        """根据文本长度动态估算 Text 组件所需行数"""
        pass

    def _create_text_widget(self, parent, text, fg_color, height):
        """创建只读 Text 组件"""
        pass
```

#### UI 设计

- **无边框窗口**：overrideredirect(True)
- **置顶**：attributes("-topmost", True)
- **自动消失**：after(10000, close) 10 秒后自动关闭（可通过配置文件调整）
- **Esc 关闭**：绑定 `<Escape>` 键事件
- **动态高度**：根据文本长度计算 Text 组件行数（最小 2 行，最大 8 行），支持换行和长文本显示
- **拖拽移动**：支持鼠标拖拽浮窗任意位置
- 浮窗出现在**鼠标光标右下方**，避免遮挡选中文本
- 包含：原文标签、动态高度原文区域、翻译标签、动态高度译文区域、朗读按钮、关闭按钮

### 5.6 tray.py -- 系统托盘

```python
class TrayApp:
    """系统托盘管理"""

    MENU_ITEMS = [
        "翻译 / 朗读",       # 触发翻译（同热键功能）
        "---",              # 分隔线
        "设置快捷键...",     # 打开热键设置
        "切换朗读引擎",      # edge / sapi5 切换
        "---",
        "开机自启",          # 开关
        "退出",
    ]

    def run(self):
        """启动托盘图标，阻塞主线程"""
        pass
```

### 5.7 config_manager.py -- 配置管理

```python
class ConfigManager:
    """JSON 配置文件读写"""

    CONFIG_PATH = os.path.join(APP_DIR, "config.json")

    DEFAULTS = {
        "hotkey": "<ctrl>+<alt>+e",
        "tts_engine": "edge",        # "edge" | "sapi5"
        "auto_close_seconds": 5,
        "youdao_app_key": "",
        "youdao_app_secret": "",
        "start_on_boot": False,
    }

    def get(self, key: str):
        pass

    def set(self, key: str, value):
        pass

    def save(self):
        pass
```

配置文件 config.json 存放在可执行文件同级目录（便携式），或 %APPDATA%/enpop/config.json。

---

## 6. 数据流

```
用户操作                    EnPop 内部               外部服务
--------------------------------------------------------------------
选中英文文本
|
按 Ctrl+Alt+E -------> hotkey 监听触发
                        |
                        capturer 模拟 Ctrl+C
                        |
                        获取剪贴板文本 ------------> 剪贴板
                        |
                        translator.translate() ----> 有道 API
                        |
                        popup.show(原文, 译文)
                        |
用户点击 喇叭 --------> tts.speak(原文) -----------> Edge TTS
                        |
5s 或 Esc ------------> popup.close()
```

---

## 7. 有道翻译 API 接入

### 7.1 注册与申请

1. 访问 [有道智云 AI 开放平台](https://ai.youdao.com)
2. 注册账号 -> 控制台 -> 创建应用
3. 选择服务：**文本翻译**
4. 获取 appKey 和 appSecret

### 7.2 API 详情

| 项目 | 值 |
|------|-----|
| 请求地址 | POST https://openapi.youdao.com/api |
| 请求格式 | application/x-www-form-urlencoded |
| 响应格式 | JSON |
| 免费额度 | 注册送 100 元体验金 |
| 计费方式 | 按字符计费（约 48元/百万字符） |

### 7.3 请求参数

| 参数 | 必填 | 说明 |
|------|------|------|
| q | 是 | 待翻译文本（UTF-8 URLEncode） |
| from | 是 | 源语言（auto 自动检测） |
| to | 是 | 目标语言（zh-CHS） |
| appKey | 是 | 应用标识 |
| salt | 是 | 随机字符串 |
| sign | 是 | 签名 sha256(appKey+q+salt+curtime+appSecret) |
| signType | 是 | v3 |
| curtime | 是 | Unix 时间戳（秒） |

### 7.4 响应示例

```json
{
  "errorCode": "0",
  "query": "Hello World",
  "translation": ["你好世界"],
  "basic": {
    "phonetic": "həˈloʊ wɜːrld",
    "explains": ["你好世界"]
  }
}
```

### 7.5 错误码

| 错误码 | 含义 | 处理方式 |
|--------|------|----------|
| 0 | 成功 | -- |
| 101 | 缺少必填参数 | 检查请求参数 |
| 103 | appKey 无效 | 检查应用配置 |
| 108 | 签名错误 | 检查 sign 生成逻辑 |
| 113 | 翻译字符超限 | 截断或拆分输入 |
| 1203 | 请求过于频繁 | 加入重试+延迟机制 |

---

## 8. 配置管理

### 8.1 配置文件位置

优先查找：

```
1. 可执行文件同级目录下的 config.json（便携模式）
2. %APPDATA%\\enpop\\config.json（安装模式）
```

### 8.2 配置文件格式

```json
{
  "hotkey": "<ctrl>+<alt>+e",
  "tts_engine": "edge",
  "auto_close_seconds": 5,
  "youdao_app_key": "",
  "youdao_app_secret": "",
  "start_on_boot": false
}
```

### 8.3 热键自定义方式

用户通过托盘菜单 -> "设置快捷键..." 打开设置窗口：

1. 弹出一个输入框，提示"按下新的快捷键组合"
2. 程序监听用户按键组合
3. 验证是否与其他全局热键冲突
4. 保存到 config.json 并重启热键监听线程

---

## 9. 开发环境搭建

### 9.1 前提条件

- Windows 10/11
- Python >= 3.10
- 网络连接（用于 pip 安装和 API 调用）

### 9.2 安装步骤

```bash
# 克隆或进入项目目录
cd D:\\code\\JavaCode\\enpop

# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
.\\venv\\Scripts\\Activate.ps1
# 或 cmd: .\\venv\\Scripts\\activate.bat

# 安装依赖
pip install -r requirements.txt
```

### 9.3 requirements.txt

```txt
pynput>=1.7.6
pyperclip>=1.8.2
requests>=2.31.0
edge-tts>=6.1.9
pyttsx3>=2.90
pystray>=0.19.4
Pillow>=10.0.0
pyinstaller>=6.0.0
```

### 9.4 运行方式

```bash
# 开发模式
python src/main.py

# 先配好 API Key
$env:ENPOP_YOUDAO_APP_KEY="your_key"
$env:ENPOP_YOUDAO_APP_SECRET="your_secret"
```

---

## 10. 构建与打包

### 10.1 PyInstaller 打包

```bash
pip install pyinstaller
cd D:\\code\\JavaCode\\enpop
pyinstaller --onefile --windowed --icon=assets\\icon.ico `
  --name EnPop `
  --add-data "assets;assets" `
  src\\main.py
```

- --onefile：打包成单 exe
- --windowed：不显示控制台窗口
- 打包产物：dist\\EnPop.exe

### 10.2 安装器（可选）

可使用 NSIS / Inno Setup 制作安装包，实现：

- 安装到 %ProgramFiles%/EnPop
- 创建开始菜单快捷方式
- 可选：添加开机启动项

---

## 11. 后续规划

### v1.0（当前目标）

- [x] 可行性讨论与技术选型
- [x] 项目结构设计
- [x] 核心功能实现：全局热键 -> 捕获 -> 翻译 -> 浮窗 -> TTS
- [x] 配置管理：快捷键自定义、切换引擎
- [x] 打包分发：单 exe

### v1.1

- [ ] 缓存已翻译内容，相同文本不重复请求 API
- [ ] 翻译历史记录（本地存储）
- [ ] 浮窗样式可配置（字体大小、颜色、透明度）

### v1.2

- [ ] UIA 文本捕获 fallback（扩展兼容性）
- [ ] 在线/离线 TTS 自动切换
- [ ] 翻译静默模式（不显示浮窗，仅朗读）

### 远期

- [ ] 截图 OCR 翻译（覆盖图片中的文字）
- [ ] 输入框翻译模式（直接输入文本翻译）
- [ ] 更多翻译源切换（Google / DeepL / 本地模型）

---

*文档版本：v1.0 - 2026-06-05*
